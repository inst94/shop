@model shop.Models.Files.ExistingFilePathCarViewModel

@{
    var photoPaths = "~/multipleFileUpload/" + (Model.FilePath ?? "noimage.jpg");
}

<div class="form-group row col-sm-4 offset-2">
    <img class="card-img-top" src="@photoPaths" asp-append-version="true" />
    <input asp-action="removeImage" asp-controller="car" asp-route-FilePath="@Model.FilePath" type="submit" class="btn btn-danger" value="Remove Image" />
</div>
